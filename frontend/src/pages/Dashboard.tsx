// Metric cards, low-stock panel, recent movements, pending POs; tenant-scoped queries.
const Dashboard = () => {
  return (
    <div className="w-full h-full  flex items-center justify-center">Dahboard</div>
  );
};

export default Dashboard;
